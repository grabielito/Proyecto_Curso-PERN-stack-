import CompraTable from '../Componentes/CompraTable'

//Este componente renderiza la vista principal a la seccion de compras o ordenes realizadas por los clientes, asi como las operaciones que se pueden realizar sobre las mismas.

const Ordenes = () => {
  
  return (
    <>
     <CompraTable/>
      
      </>
  )
}

export default Ordenes