import ClienteTable from '../Componentes/ClienteTable'


//Este componente renderiza la ruta principal de la seccion clientes donde se muestran todos los datos y operaciones sobre el mismo.

const Clientes = () => {
  
  return (
    <ClienteTable/>
  )
}

export default Clientes